# Exe builder
PyInstaller

# Core GUI Presentation Layer
PySide6>=6.5.0

# Node Graph Framework for Process Flow Editor
# Note: On Python 3.12+, setuptools is required because NodeGraphQt references distutils.version
NodeGraphQt>=0.6.44

# Compatibility shim providing distutils for NodeGraphQt on Python 3.12+
setuptools>=65.0.0

# Persistence Layer ORM & Metadata Catalog
SQLAlchemy>=2.0.0

# BigQuery Integration & Data Processing
google-cloud-bigquery
pandas
openpyxl>=3.1.0
pyarrow
db-dtypes
sqlglot>=20.0.0
