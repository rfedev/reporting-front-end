
Process flows are no longer saving correctly. If I edit one and save, then return to it, it's empty.

When dragging and dropping a query into the main process flow canvas from the left panel, make sure it is placed in the location it's dropped.

The 'Appearance' menu should have 3 horizontal lines to denote a menu, rather than the down arrow. There should be a small space between the work 'Appearance' and the 3 line icon.

The vertical and horizontal alignment is not working anymore, it's not including all tables and query boxes. If nothing is selected, it should align all objects. If objects are selected, only align the selected objects.

When double clicking an output csv box, each output file should have a file picker to allow selecting an ouput file/folder.

For the 'fit graph' button, If nothing is selected, it should fit around all objects. If objects are selected, only fit round the selected objects.

Import csv boxed should be able to be deleted with the delete key.

In the import csv dialog window, when a csv file is selected, the ouput table text box should be automatically populated with 'projectid.dataset.csvtablename' where the 'projectid.dataset.' is the default working dataset set in the settings and csvtablename is the name of the csv without the .csv extension.



In the import csv dialog window, each csv file and ouput table should be contained (next to each other vertically) in a section. The 'add csv' button is not adding a new section to allow multiple csv imports.
